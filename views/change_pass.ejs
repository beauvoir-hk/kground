<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>케이그라운드</title>
    <%- include("header.ejs") %>
    <script>
        
        function authok(){ //state==1
            alert("인증이 완료되었습니다. 비밀번호를 변경하세요")
        }
        function passchangeok(){
            alert("로그인 비밀번호가 변경되었습니다")
        }
        </script>
    
</head>
<% if(state==1){%>
    <body class="bg-gradient-primary" onload="authok()">
        <%}else{ if(state==2){%>
            <body class="bg-gradient-primary" onload="authok()" >
                <%}else{%>
        <body class="bg-gradient-primary"  >
            <%}}%>
        <div class="container">
            <div class="text-center">
                <a id="signinlink" href="/index" >홈으로</a>
            </div>
        
        <!-- Outer Row -->
        <div class="row justify-content-center">
            <div class="col-xl-10 col-lg-12 col-md-9">
                <div class="card o-hidden border-0 shadow-lg my-5">
                    <div class="card-body p-0">
                        <!-- Nested Row within Card Body -->
                        <div class="row">
                            <div class="text-center">
                                <br>
                                
                                <a id="signinlink" href="/index" >홈으로</a>
                            </div>
                            <div class="col-lg-6"> 
                                <div class="p-5">
                                    <div class="text-center">
                                        <img src="/img/k.png" width="10%">
                                        <h6>로그인 + 결제비밀번호 변경</h6>
                                    </div>

                                    <!-- 유저가 회원 정보를 입력하는 공간 -->
                                    <form action="/change_pass_all", method="post">
                                        <div align="right">  
                                            <span class="small"name="_phone" value="<%=_phone%>" >전화번호 : <%=_phone%></span>
                                        </div>

                                        <br>[로그인 비밀번호 변경]

                                        
                                            <div class="form-group">
                                                <input type="password" name="input_pass" id="_pass" class="form-control" placeholder="비밀번호"  >
                                            </div>
                                            <div class="form-group">
                                                <input type="password" id="_confirm" class="form-control" placeholder="비밀번호 확인"  >
                                            </div>
                                            <div align="right">  
                                                <span class="small">※ 비밀번호 일치 시 비밀번호변경 버튼 활성</span>
                                            </div>
                                            <br>
                                    <br>
                                    <br>[결제비밀번호변경]
                                      <div align="right" style="display: none;">  
                                            <span class="small" name="_phone" value="<%=_phone%>"><%=_phone%></span>
                                        </div>

                                            <div class="form-group">
                                                <input type="password" name="input_paypass6" id="_paypass6" class="form-control" placeholder="비밀번호"  >
                                            </div>
                                            <div class="form-group">
                                                <input type="password" id="_confirm6" class="form-control" placeholder="비밀번호 확인"  >
                                            </div>

                                            <div align="right">  
                                                <span class="small">※ 비밀번호 일치 시 비밀번호변경 버튼 활성</span>
                                            </div>
                                            <br>
                                    
                                            <div class="display" id="info">
                                            <br>
                                            <div class="text-center">  
                                                <input  type="submit" value="로그인 + 결제비밀번호변경" id="signup6" disabled>
                                            </div>
                                        </div>
                                    </form>
                                </div>
                            </div>  
                            <div class="col-lg-5"><img src="/img/kk.png"  width="100%" ></div> 
                        </div>
                    </div>
                </div>
            </div>
        </div>
        </div>    
    <script>
        $('#_confirm6').keyup(function(){
            const password6 = $('#_paypass6').val()
            const confirm6 = $('#_confirm6').val()
            if(password == confirm&&password6 == confirm6){
                $('#signup6').removeAttr('disabled')
            }else{
                $('#signup6').attr('disabled', 'true')
            }
        })
        // 패스워드와 확인 패스워드가 같아야지만 회원가입 버튼이 활성화
        // 태그의 id가 _confirm이 입력이 될때마다 _pass와 값을 비교하여 
        // 같은 경우에 회원가입 버튼을 활성화
        $('#_confirm').keyup(function(){
            const password = $('#_pass').val()
            const confirm = $('#_confirm').val()
            if(password == confirm){
                $('#signup').removeAttr('disabled')
            }else{
                $('#signup').attr('disabled', 'true')
            }
        })
    </script>
</body>
</html>